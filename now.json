{
  "version": 2,
  "builds": [{ "src": "/server/src/index.ts", "use": "@now/node" }],
  "routes": [{ "src": "/graphql", "dest": "/server/src/index.ts" }],
  "env": {
    "POSTGRESQL_URL": "@rewarp-postgresql-url"
  },
  "build": {
    "env": {
      "POSTGRESQL_URL": "@rewarp-postgresql-url"
    }
  }
}
